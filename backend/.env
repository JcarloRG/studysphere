# ================================
# === DJANGO CONFIGURACIÓN BASE ===
# ================================
DJANGO_SECRET_KEY=django-insecure-clave-temporal
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# =====================
# === BASE DE DATOS ===
# =====================
DB_ENGINE=django.db.backends.mysql
DB_NAME=studysphere
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306

# ===========================
# === CONFIGURACIÓN EMAIL ===
# ===========================
# Servidor SMTP de Gmail (usa una cuenta con 2FA + contraseña de aplicación)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
EMAIL_HOST_USER=studysphere.notification@gmail.com          # <- el correo real
EMAIL_HOST_PASSWORD=cuevrsxjiblfqlvj                           # <- contraseña de APLICACIÓN (16 caracteres)
DEFAULT_FROM_EMAIL=studysphere.notification@gmail.com        # <- debe coincidir con HOST_USER


# ==============================
# === CONFIGURACIÓN FRONTEND ===
# ==============================
# Para armar enlaces o redirecciones desde el backend
FRONTEND_BASE_URL=http://localhost:3000

# =======================================
# === DOMINIOS DE CORREO PERMITIDOS ====
# =======================================
# Solo se aceptarán estos dominios al registrar usuarios
ALLOWED_EMAIL_DOMAINS=gmail.com,hotmail.com,live.com,outlook.com,itsx.edu.mx
BLOCKED_EMAIL_DOMAINS=

# ============================
# === CONFIGURACIÓN DE CORS ==
# ============================
# Permite que React (localhost:3000) pueda hacer peticiones al backend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=true

# ====================================
# === CONFIGURACIÓN VERIFICACIÓN EMAIL
# ====================================
# Tiempo en minutos antes de que el código expire
EMAIL_VERIFICATION_EXP_MINUTES=15

# =====================
# === OPCIONAL LOG ===
# =====================
# Si deseas ver los correos en consola mientras pruebas, usa:
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# (Solo activarlo temporalmente si aún no quieres enviar correos reales)
